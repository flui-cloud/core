apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{CERT_NAME}}
  namespace: {{NAMESPACE}}
  labels:
    managed-by: flui-cloud
spec:
  secretName: {{SECRET_NAME}}
  issuerRef:
    name: {{ISSUER_NAME}}
    kind: ClusterIssuer
  dnsNames:
    {{DNS_NAMES}}
